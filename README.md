# Fitfinity Staff PWA Preview v0.24

- Repaired Create Trainer availability formatting on mobile.
- Trainer name stays fixed/read-only during owner edits and is styled as compact identity text.
- Remuneration trainer detail removes the redundant Trainers button and uses a smaller name + cycle heading.
- PWA cache bumped to v24.

Prototype only; use dummy data until backend authentication and persistence are connected.
