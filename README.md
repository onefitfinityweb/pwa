# Fitfinity PWA Preview v0.21

UI cleanup build for mobile testing.

Changes:
- Client Progress is constrained to the phone width with no horizontal scrolling for owner or trainer views.
- Trainer detail > Assigned Clients uses a stacked mobile row so package information cannot force horizontal overflow.
- Removed all app-level swipe-back visual/floating arrow cues; swipe-back itself remains enabled.
- Top-left Back control is now a clean chevron.
- PWA cache bumped to v21.

Prototype only: use dummy data until the real authenticated backend is connected.
